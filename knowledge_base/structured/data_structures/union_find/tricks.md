- 使用路径压缩和按秩合并可将时间复杂度优化至 Θ(m α(n))。
- 若不想实现按秩合并，可用 `log2(size)` 代替秩进行启发式合并，仍能保证相近的复杂度。
- 启发式合并（按大小合并）中，每次将较小的集合合并到较大的集合上，确保每个节点最多被移动 O(log n) 次。
- 可通过维护 `size` 数组实现对集合大小的追踪，便于后续扩展为带权并查集。
- 在某些题目中，并查集可用于缩点或预处理连通块，从而简化图结构。