- **维护严格单调性**：使用 `>=` 或 `>` 根据题意决定是否允许相等元素。
- **记录索引而非值**：通常将数组下标入栈，便于计算距离或定位。
- **模拟插入时的弹出逻辑**：每次插入新元素前，从栈顶不断弹出破坏单调性的元素，保证栈内始终有序。
- **与前后关联问题结合**：可用于寻找每个元素左右两边第一个更大或更小的元素，典型应用在“柱状图中最大矩形”问题中。
- **时间复杂度均摊分析**：每个元素最多入栈和出栈一次，整体时间复杂度为 O(n)。