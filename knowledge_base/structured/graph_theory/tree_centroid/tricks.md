- 树的重心最多有两个，且若有两个则它们相邻。
- 添加或删除一个叶子节点时，重心最多移动一条边的距离。
- 两棵树通过一条边连接后，新树的重心位于原两棵树重心之间的路径上。
- 在有根树中，子树的重心一定在从其子树重心到当前根的路径上，可用于递推求解所有子树的重心。
- 求重心时可通过 DFS 统计子树大小，并用 `max(子树最大大小, n - 当前子树大小)` 判断是否为重心。
- 换根 DP 可用于计算每个点作为根时的深度和，最小值对应重心（基于距离和最小的等价定义）。