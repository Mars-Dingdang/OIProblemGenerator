- **双端队列 BFS**：适用于边权仅为 0 或 1 的情况。当边权为 0 时，从队首插入；边权为 1 时，从队尾插入，保证队列单调性，从而维护最短路。
- **堆优化 Dijkstra 是优先队列 BFS**：在带权图中使用优先队列扩展节点，本质是广义的 BFS。
- **层序遍历区分层次**：通过 `for (int i = 0, n = q.size(); i < n; ++i)` 控制每层节点数，用于输出每层节点或计算树高。
- **Morris 遍历启发**：利用空指针临时建立线索，避免使用栈空间，实现 O(1) 空间复杂度的遍历（仅限二叉树）。