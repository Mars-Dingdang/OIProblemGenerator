- 小端序（little endian）：低有效字节存储在低地址，高有效字节存储在高地址。常见于x86、x86_64架构。
- 大端序（big endian）：高有效字节存储在低地址，低有效字节存储在高地址。常见于网络传输（如IP、TCP协议）和某些嵌入式系统。
- 判断字节序可通过将整数1的地址强制转换为字符指针，检查最低地址处的值是否为1。
- 在跨平台数据交换或网络编程中，需注意字节序转换，常用函数包括 `htonl`, `htons`, `ntohl`, `ntohs`。
- 对于多字节数据类型（如int、float），不同字节序会影响内存布局，但不会影响单字节操作。